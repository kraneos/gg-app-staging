before_script:
#  - apt-get update -qq && apt-get install -y -qq nodejs
  - npm -v
  - node -v
  - npm install

pages:
  script:
    - ng build -o public
  artifacts:
    - public
  only:
    - pages
