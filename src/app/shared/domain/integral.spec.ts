/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {Integral} from './integral';

describe('Integral', () => {
  it('should create an instance', () => {
    expect(new Integral()).toBeTruthy();
  });
});
