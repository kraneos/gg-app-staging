/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {PasRegistrationUser} from './pas-registration-user';

describe('PasRegistrationUser', () => {
  it('should create an instance', () => {
    expect(new PasRegistrationUser()).toBeTruthy();
  });
});
