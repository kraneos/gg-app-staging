/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {LedgerAccountsQueryOptions} from './ledger-accounts-query-options';

describe('LedgerAccountsQueryOptions', () => {
  it('should create an instance', () => {
    expect(new LedgerAccountsQueryOptions()).toBeTruthy();
  });
});
